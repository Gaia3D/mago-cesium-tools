/**
 * VolumetricData class
 */
export class VolumetricData {
    /**
     * Creates an instance of VolumetricData.
     * @param options
     */
    constructor(options: any);
    mosaicTexture: any;
    /**
     * Sets the mosaic texture for the volumetric data.
     * @param texture
     */
    setMosaicTexture(texture: any): void;
}
